# [572. Subtree of Another Tree](https://leetcode.com/problems/subtree-of-another-tree/?envType=problem-list-v2&envId=plakya4j)
**Level**：
Easy

**Topics**： 
Senior Staff
Tree
Depth-First Search
String Matching
Binary Tree
Hash Function

**First AC**：  
Aug 19, 2026

## Idea
Though this is an **Easy** problem, it is not as easy as it seems for me. There are several cases to deal with, so the best way to solve this problem is to sit down and write all the cases on paper. 

I am working on these problems in fragment of time, so I failed to deal with this problem within a reasonable time.

## Code
None -> Go to #Ideal_Solution for answer.
```python

```

## Ideal Solution
The idea is not hard -- use a function `isSame` to check if `root` is the head of the sub-tree that we are looking for. 

The function it self is just to walk through all the nodes.

That's all. Remember that you have to check both the structure of the trees and the value of these nodes.

```python
# Definition for a binary tree node.
# class TreeNode:
#     def __init__(self, val=0, left=None, right=None):
#         self.val = val
#         self.left = left
#         self.right = right
class Solution:
    def isSubtree(self, root: Optional[TreeNode], subRoot: Optional[TreeNode]) -> bool:
        def isSame(root, subRoot):
            if not root and not subRoot:
                return True
            elif not root or not subRoot:
                return False
            elif root.val != subRoot.val:
                return False
            return isSame(root.left, subRoot.left) and isSame(root.right, subRoot.right)
        if not root:
            return False
        elif isSame(root, subRoot):
            return True
        else:
            return self.isSubtree(root.left, subRoot) or self.isSubtree(root.right, subRoot)
```