# [24. Swap Nodes in Pairs](https://leetcode.com/problems/swap-nodes-in-pairs/description/)
**Level**：
Medium

**Topics**： 
Linked List
Recursion

**First AC**：  
Aug 10, 2026

## Idea
This problem is not hard, but there is one thing important: remember to use `prev` to manage the end of the last pair. 

That is to say, since we are swapping the two nodes, if we are not assigning `prev.next = next_curr`, the last node does not know which node to connect to. 

**Time complexity: $\theta(n)$; Space Complexity: $\theta(n)$**

## Code
```python
# Definition for singly-linked list.
# class ListNode(object):
#     def __init__(self, val=0, next=None):
#         self.val = val
#         self.next = next
class Solution(object):
    def swapPairs(self, head):
        """
        :type head: Optional[ListNode]
        :rtype: Optional[ListNode]
        """
        curr = head
        if curr is None:
            return curr
        elif curr.next is None:
            return curr
        next_curr = curr.next
        prev = ListNode(None)
        result = prev
        while curr.next is not None:
            prev.next = next_curr
            curr.next = next_curr.next
            next_curr.next = curr
            prev = curr
            curr = curr.next
            if curr is not None:
                next_curr = curr.next
            else:
                break
        return result.next
```

## Ideal Solution
Use recursion.

Base case is when there is no node or only one node left. 
Then store `temp` as the new node after swap.
After stored `temp`, then head.next is no longer `temp`, but the following all nodes after swapped. This is just the `prev` node as in my own solution.
Finally connect head to temp.next to swap thes two nodes.

```python
class Solution:
    def swapPairs(self, head: ListNode) -> ListNode:
        if not head or not head.next:  # Base case
            return head
        
        temp = head.next  # Store the second node
        head.next = self.swapPairs(temp.next)  # Recursively swap the rest of the list
        temp.next = head  # Swap current pair
        
        return temp  # New head of the swapped pair
```